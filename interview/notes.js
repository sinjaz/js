Array
.push (add)
.unshift (add)
.pop (remove)
.shift (remove)
.splice (i, n, obj)

String
.slice (i, j)
.substring (i, j)
.substr(i, n)
.charAt ()

Inheritance
const a = {}
const b = Object.create(a);

Set
.add()
.delete()
.entries()
.has()
.values()

Map
.size()
.get()
.set()
.has()
.forEach()
.keys()
.values()
.delete()
.entries()
// for -- of
